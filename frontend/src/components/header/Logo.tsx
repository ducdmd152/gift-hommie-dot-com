import {
  Text,
  useColorModeValue,
  useBreakpointValue,
  Heading,
} from "@chakra-ui/react";
import React from "react";

const Logo = () => {
  return (
    <Heading size="md" letterSpacing={2} fontWeight="medium">
      HOMMIE
    </Heading>
  );
};

export default Logo;
